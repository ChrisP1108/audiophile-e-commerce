export interface newProductInterface {
    id: number;
    name: string;
    category: string;
    slug: string;
}

export interface newProductDataInterface {
    id: number;
    name: string;
    headline: string;
    buttonText: string;
    buttonRoute: string;
}